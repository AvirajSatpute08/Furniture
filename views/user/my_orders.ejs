<!-- <%- include('navbar.ejs') %>

<div class="hero">
    <div class="container">
        <div class="row justify-content-between">
            <div class="col-lg-5">
                <div class="intro-excerpt">
                    <h1>My Orders</h1>
                </div>
            </div>
        </div>
    </div>
</div>

<div class="container-fluid">
    <div class="row">
        <% if (orders.length > 0) { %>
            <% for(let i = 0; i < orders.length; i++) { %>
                <div class="col-md-4 mt-3">
                    <a href="/print_order/<%= orders[i]._id %>" style="text-decoration: none;">
                        <div class="order_box shadow border p-3 mt-4" style="color: black;">
                            <div style="text-align: center; text-transform: capitalize; background-color:#385D50; color: white;">
                                <%= orders[i].order_status %>
                            </div>
                            #FURNI <%= orders[i]._id %>
                            <span class="float-end">
                                <%= orders[i].order_date %>
                            </span><br>
                            &#8377; <%= orders[i].total_amt %>
                            <span class="float-end" style="text-transform: capitalize;">
                                Payment: <%= orders[i].payment_status %>
                            </span>
                        </div>
                    </a>
                </div>
            <% } %>
        <% } else { %>
            <div class="col-md-12 mt-3">
                <p>No orders found.</p>
            </div>
        <% } %>
    </div>
</div>

<br><br><br><br><br>

<%- include('footer.ejs') %> -->



<%- include('navbar.ejs') %>

<div class="hero">
    <div class="container">
        <div class="row justify-content-between">
            <div class="col-lg-5">
                <div class="intro-excerpt">
                    <h1>My Orders</h1>
                </div>
            </div>
        </div>
    </div>
</div>

<div class="container-fluid">
    <div class="row">
        <% if (orders.length > 0) { %>
            <% for(let i = 0; i < orders.length; i++) { %>
                <div class="col-md-4 mt-3">
                    <a href="/print_order/<%= orders[i]._id %>" style="text-decoration: none;">
                        <div class="order_box shadow border p-3 mt-4" style="color: black;">
                            <div style="text-align: center; text-transform: capitalize; background-color:#385D50; color: white;">
                                <%= orders[i].order_status %>
                            </div>
                            #FURNI <%= orders[i]._id %>
                            <span class="float-end">
                                <%= orders[i].order_date %>
                            </span><br>
                            &#8377; <%= orders[i].total_amt %> <!-- Ensure this shows the correct total amount -->
                            <span class="float-end" style="text-transform: capitalize;">
                                Payment: <%= orders[i].payment_status %>
                            </span>
                        </div>
                    </a>
                </div>
            <% } %>
        <% } else { %>
            <div class="col-md-12 mt-3">
                <p>No orders found.</p>
            </div>
        <% } %>
    </div>
</div>

<%- include('footer.ejs') %>

